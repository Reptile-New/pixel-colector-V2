{
  "name": "pixel-collector",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Pixel Collector \u2014 arcade collector / press-your-luck roguelite",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --port 4173",
    "typecheck": "tsc --noEmit",
    "build:single": "vite build --config vite.single.config.ts && node scripts/bundle-single.mjs"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
